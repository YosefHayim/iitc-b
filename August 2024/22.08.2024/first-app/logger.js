function log(message) {
    // Send an HTTP request
    console.log(message);
    
}

// Exporting the entire logger.js file.
// module.exports.log = log;

// Exporting only the log function
// module.exports = log;
