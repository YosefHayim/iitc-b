// Recursive Factorial Challenge
// ------------------------------
//
// Problem Description:
// Write a recursive function that computes the factorial of a number.
// Factorial of n (n!) is the product of all positive integers up to n.
// Note: factorial(0) is defined as 1.
//
// Approach Tips:
// 1. Define the base case: if n is 0, return 1.
// 2. For n greater than 0, return n multiplied by factorial(n-1).
// 3. Consider error handling for negative inputs.
// 4. Test with small numbers (like 5, which should yield 120) as well as edge cases.
function factorial(n) {
  // TODO: Check for the base case: if n is 0, return 1.
  // TODO: For n > 0, return n * factorial(n - 1).
  // TODO: Optionally handle negative numbers with a proper response.
}
