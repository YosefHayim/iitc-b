// Anagram Checker Challenge
// --------------------------
//
// Problem Description:
// Create a function that checks if two strings are anagrams of each other.
// An anagram is formed by rearranging the letters of one string to produce another,
// ignoring spaces, punctuation, and capitalization.
//
// Approach Tips:
// 1. Normalize both strings by converting them to lowercase and removing non-alphanumeric characters.
// 2. Sort the characters of each string and compare the sorted versions.
// 3. Return true if the sorted strings are equal; otherwise, false.
// 4. Test with pairs that are anagrams (e.g., "listen" and "silent") and non-anagrams.
function areAnagrams(str1, str2) {
  // TODO: Normalize both strings (e.g., convert to lowercase and remove non-alphanumeric characters).
  // TODO: Split the strings into arrays, sort them, and join them back.
  // TODO: Compare the sorted strings and return the result.
}
